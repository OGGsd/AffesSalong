export default function TechnicalSEO() {
  // Technical SEO meta tags are now handled by the Metadata API and headers in next.config.mjs
  // This component is kept for compatibility but doesn't render anything
  return null
}