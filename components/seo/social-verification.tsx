export default function SocialVerification() {
  // Social verification meta tags are now handled by the Metadata API in layout.tsx
  // This component is kept for compatibility but doesn't render anything
  return null
}